-------Python import, module and init file--------

In Python, a module is a file containing Python code (with a .py extension) that can define functions, classes, and variables. You bring this code into another script using the import keyword, which promotes code reuse and cleaner project organization
